import time

def Runtime():
    def __init__(self, name, num_cycles, num_breathes, cycle_time):
        self.name = name
        self.num_cycles = num_cycles
        self.num_breathes = num_breathes
        self.cycle_time = cycle_time

    def intro(self):
        time.sleep(10)
        print("Hello " + self.name)